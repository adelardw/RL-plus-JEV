# План эксперимента: источники награды для RL-дообучения малой LLM

## 1. Вопрос

Как **источник награды** влияет на качество, устойчивость к reward hacking и стоимость RL-дообучения при фиксированном оптимизаторе — и сохраняется ли результат при смене оптимизатора (GRPO → PPO).

Сравниваем четыре (+1 опциональный) источника:

- классический reward model (DeBERTa/BERT-классификатор на человеческих предпочтениях)
- self-judge (модель оценивает свои ответы)
- LLM-судья (RLAIF)
- Jev (RLJEV-as-a-RF)
- *опционально:* внутренняя награда без внешнего судьи (self-certainty)

## 2. Гипотезы (фиксируем до запуска)

| # | Гипотеза |
|---|---|
| H1 | Jev ≈ LLM-судья по качеству на eval при кратно меньшей стоимости |
| H2 | Self-judge на 0.5–1.5B даёт шумную награду и минимальный прирост |
| H3 | BERT RM стабилен, но упирается в потолок (512 токенов, домен предпочтений) |
| H4 | Jev и LLM-судья хакаются раньше BERT RM, если в рубрике мало негативных вопросов |
| H5 | Ранжирование источников сохраняется при переходе GRPO → PPO |
| H6 | Внутренняя награда (self-certainty) на открытых диалоговых задачах уступает внешним источникам, на GSM8K — конкурентна |

## 3. Дизайн

### Фаза 1 — GRPO, меняется только функция награды

| Run | Награда | Судья |
|---|---|---|
| R0 | — | SFT-чекпойнт, точка отсчёта |
| R1 | BERT RM | `OpenAssistant/reward-model-deberta-v3-large-v2` или свой BT-RM |
| R2 | self-judge | **замороженная** копия SFT, общая рубрика |
| R3 | LLM-судья | 7–8B instruct, общая рубрика |
| R4 | Jev | общая рубрика |
| R2b *(опц.)* | self-judge live | текущая политика как судья (true self-rewarding) |
| R5 *(опц.)* | self-certainty | уверенность самой политики, без внешнего судьи |
| B *(опц.)* | DPO | офлайн-бейзлайн на тех же парах |

### Фаза 2 — PPO

Минимум: R1 (BERT RM) и R4 (Jev). При наличии бюджета — все источники из фазы 1.

## 4. Контролируемые переменные

Идентичны во всех прогонах:

- стартовый SFT-чекпойнт
- набор промптов и их порядок (фиксированный seed даталоадера)
- параметры сэмплирования (temperature, top_p, max_completion_length)
- KL-коэффициент / KL-таргет
- число сэмплов на промпт (G) для GRPO
- **бюджет вызовов награды** (а не число шагов)
- сиды: ≥3 на прогон

## 5. Стек

| Компонент | Выбор |
|---|---|
| Политика | Qwen 0.5B или 1.5B Instruct |
| Промпты | UltraFeedback (binarized), при желании HH-RLHF |
| Задача с ground truth | GSM8K (детектор хака и capability tax) |
| RM | DeBERTa-v3 RM (готовый или BT на тех же парах) |
| LLM-судья | 7–8B instruct через vLLM |
| Jev | API TypeSafe, `jev-latest` |
| Обучение | TRL: `GRPOTrainer`, `PPOTrainer`, `RewardTrainer`, `DPOTrainer` |
| Генерация | vLLM |
| Трекинг | W&B / MLflow |

Железо: 1× 80GB под политику + генерацию, 1× GPU под 7–8B судью. PPO на 1.5B держит 4 модели (policy, ref, RM, value) — впритык; 0.5B спокойно.

## 6. Функции награды

Единая сигнатура, тренер не меняется:

```python
def reward_bert(prompts, completions, **kw) -> list[float]: ...
def reward_self(prompts, completions, **kw) -> list[float]: ...
def reward_llm (prompts, completions, **kw) -> list[float]: ...
def reward_jev (prompts, completions, **kw) -> list[float]: ...
def reward_certainty(prompts, completions, **kw) -> list[float]: ...  # R5

trainer = GRPOTrainer(model=sft_ckpt, reward_funcs=reward_jev, args=cfg, ...)
```

### 6.1 Общая рубрика (R2, R3, R4)

| Вопрос | Тип | Знак |
|---|---|---|
| `correct` — отвечает ли ответ на вопрос корректно | noul | + |
| `helpful` — насколько полезен (Useless / Partial / Complete) | score, 3 уровня | + |
| `padded` — есть ли вода и повторы | noul | − |
| `evasive` — уходит ли от ответа | noul | − |
| `self_promo` — делает ли заявления о собственном качестве | noul | − |

```
reward = p_correct + helpful / 2 − p_padded − p_evasive − p_self_promo
```

`self_promo` обязателен: ответ политики лежит внутри state судьи, и политика может научиться писать фразы, которые судья читает как доказательство качества.

### 6.2 Честность сравнения судей

- **Никакого парсинга текста** у self- и LLM-судьи. Для noul: `P("Yes") / (P("Yes") + P("No"))`. Для score: softmax по токенам уровней → матожидание. Так награда плотная и без ошибок парсинга — по форме как у Jev.
- **Вопросы изолированы** и у self/LLM-судьи: каждый вопрос — отдельный prefill с общим префиксом (prefix caching), как у Jev.
- Confidence-гейтинг есть только у Jev → выносим в отдельную абляцию R4-gated, в основном R4 не используем.

### 6.3 BERT RM

- Лимит 512 токенов: промпт обрезаем слева, ответ сохраняем целиком.
- Выход — скалярный логит; для PPO обязателен whitening.

### 6.4 R5 — внутренняя награда

Средняя уверенность политики в своих токенах (self-certainty, KL от равномерного распределения по словарю). Внешнего судьи нет. Ожидаем коллапс к коротким уверенным ответам на открытых задачах — логируем длину и энтропию особенно внимательно.

## 7. Стартовые гиперпараметры

Не тюнены, это точка входа.

**GRPO**

| Параметр | Значение |
|---|---|
| G (сэмплов на промпт) | 8 |
| промптов на шаг | 64 |
| шагов | 1000 |
| lr | 1e-6 |
| beta (KL) | 0.04 — задать явно, дефолт TRL менялся между версиями |
| max_completion_length | 512 |
| temperature | 1.0 |

**PPO**

| Параметр | Значение |
|---|---|
| сэмплов на промпт | 1 |
| промптов на шаг | подобрать так, чтобы суммарные вызовы награды = GRPO |
| lr policy / value | 1e-6 / 1e-5 |
| KL coef | 0.05 |
| reward whitening | да |

## 8. Оценка

### 8.1 Основные метрики

- **AlpacaEval 2 LC win-rate** против R0 с судьёй, **не совпадающим** ни с одним судьёй обучения (другое семейство моделей, не Jev).
- *Опционально:* Arena-Hard.
- Все сравнения — **при одинаковом KL(π‖π_ref)**: точки KL ≈ 2, 5, 10 nats. Не при одинаковом числе шагов.

### 8.2 Детекторы хака

- GSM8K accuracy (падение = модель учит судью, а не задачу)
- длина ответов по шагам
- частота фраз самопохвалы
- разрыв «награда на обучении ↑ / eval плоский»

### 8.3 Матрица перекрёстных наград

Каждый финальный чекпойнт (R1–R5) оценить **всеми** источниками награды → матрица 5×5. Диагональ vs вне диагонали показывает, насколько каждая модель переоптимизирована под свой источник.

### 8.4 Стоимость

- $ на прогон
- GPU-часы и wall-clock
- число вызовов награды
- латентность награды p50 / p95

### 8.5 Статистика

- ≥3 сида, mean ± std
- bootstrap CI на win-rate

## 9. Бюджет

| Источник | Прикидка на один прогон |
|---|---|
| Jev | 1000 × 64 × 8 ≈ 512k вызовов × ~500 ток ≈ 256M ток ≈ **~$11** (×3 сида ≈ $32) |
| LLM-судья 7–8B | локально, 5 prefill на сэмпл с prefix caching; основная цена — GPU-часы |
| Self-judge | локально, дешевле LLM-судьи |
| BERT RM | локально, почти бесплатно |

## 10. Риски

| Риск | Что делаем |
|---|---|
| Доступ к Jev (вейтлист), rate limits | запросить заранее; очередь с ретраями |
| ToS на обучение по выходам API | проверить до старта |
| Слабый self-judge на малой модели | это результат, фиксируем как есть |
| Нестабильность PPO | whitening, клиппинг, меньший lr |
| Length bias у eval-судьи | AlpacaEval LC |
| Разные шкалы наград | GRPO нормализует в группе; для PPO — whitening |

## 11. Порядок работ

1. SFT-чекпойнт, базовый eval R0
2. R1 (BERT + GRPO) — отладка пайплайна end-to-end
3. R4 (Jev), R3 (LLM-судья), R2 (self)
4. Анализ: кривые quality-vs-KL, матрица 5×5, стоимость
5. Опциональные прогоны: R2b, R5, R4-gated, DPO
6. Фаза 2: PPO на R1 и R4
7. Отчёт

## 12. Шаблон таблицы результатов

| Run | Награда | Оптим. | Win-rate @KL=5 | GSM8K Δ | Длина Δ | $ | GPU-ч |
|---|---|---|---|---|---|---|---|
| R0 | — | — | 50% | 0 | 0 | 0 | 0 |
| R1 | BERT RM | GRPO | | | | | |
| R2 | self-judge | GRPO | | | | | |
| R3 | LLM-судья | GRPO | | | | | |
| R4 | Jev | GRPO | | | | | |
| R5 | self-certainty | GRPO | | | | | |
| R1-PPO | BERT RM | PPO | | | | | |
| R4-PPO | Jev | PPO | | | | | |
